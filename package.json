{
  "name": "collaborative-za-sample",
  "version": "1.0.0",
  "description": "A Zoom App Template",
  "private": true,
  "license": "MIT",
  "main": "dist/index.js",
  "workspaces": [
    "server",
    "app"
  ],
  "scripts": {
    "start": "node dist/index.js",
    "build": "node scripts/build.sh",
    "dev": "node scripts/dev.cjs",
    "prepare": "husky install",
    "prestart": "npm run build",
    "postinstall": "node ./scripts/gen-secrets.js"
  },
  "devDependencies": {
    "concurrently": "^7.1.0",
    "dotenv-cli": "^5.1.0",
    "envsub": "^4.0.7",
    "husky": "^7.0.4"
  },
  "lint-staged": {
    "*.ts": [
      "eslint --cache --fix",
      "prettier --write"
    ]
  }
}
